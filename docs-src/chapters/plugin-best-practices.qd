.docname {Plugin Best Practices}
.docauthor {BakingRL}
.include {docs}
.doclang {en}

This guide turns the V4 runtime model into practical authoring rules. The goal
is to keep packages predictable in the host, safe at runtime, and easy to
maintain as the runtime API evolves.

## Choose The Right Surface

Use a webview for host-opened browser UI such as tools, dashboards, settings
panels, and package-owned control surfaces.

Use a service for backend state. Services are the right place to aggregate
telemetry, publish package events, write registry snapshots, expose RPC methods,
and keep state that several webviews or dependent packages need.

Use a native sidecar for heavier trusted work or platform-specific integration.
Keep the public API small and expose the useful surface through an
extension-host service.

Use extension points, contributions, and resources when a package is meant to
be extended by other packages. Keep contribution metadata generic and let the
platform plugin interpret domain-specific meaning.

Use host-owned settings for package configuration. Custom configuration webviews
can be referenced through `contributes.settings.ui` when the webview declares
`kind: "settings"`. Secret values must still stay host-owned; custom
configuration surfaces should use
`context.configuration.secrets` only for configured state plus set/clear flows.
Tool and panel webviews can read non-secret settings, but only settings
webviews should save settings or manage secret configured state.

## Design The Runtime Data Flow

Prefer one clear state owner. For telemetry-derived package state, subscribe in
a service, normalize the data, write a registry snapshot, and publish a
`plugin.<package-id>.*` event when webviews or dependent packages should update
immediately.

Keep webview code instance-local. Store DOM nodes, latest render state, cleanup
callbacks, and settings per `context.root`.

Use service RPC for explicit requests, such as `snapshot`, `configure`, or
`reset`. Use bus events for streams and notifications. Use the registry for the
latest known shared value.

Keep RPC inputs and outputs JSON-compatible. They cross a host-mediated
boundary and should be easy to validate, serialize, and inspect.

## Design Webviews As Runtime Clients

Render inside `context.root` and treat that element as the sizing boundary.
Use `context.dimensions` when layout depends on the host-opened window size.

Keep browser entries portable. Do not assume filesystem paths, global routes,
or host-owned overlay/page chrome. Use package resources, services, the event
bus, and the SDK bridge for data and coordination.

Return cleanup callbacks from `mount` when a webview subscribes to events,
starts timers, or attaches DOM listeners.

## Keep Access Narrow

Use only the events, registry keys, network hosts, and storage paths the package
needs.

Use package-scoped names:

- bus publish: `plugin.<package-id>.*`;
- registry keys: `plugin.<package-id>.*`;
- storage: relative paths such as `state.json` or `history/*`.

Declare only the mediated permissions the package needs. Network declarations
communicate consent and review intent; they do not constrain direct Node or
sidecar networking.

If a package needs to read another package registry namespace, declare a
dependency on that package and prefer service/resource APIs for richer shared
state.

Treat telemetry, settings, registry values, RPC input, and webview messages as
untrusted. Validate or narrow values before using them in rendering, state
transitions, network calls, or persisted data.

Do not store secrets in package source, JSON settings, registry values, storage
files, generated bundles, logs, screenshots, or test fixtures. Read secrets
through `context.secrets` only when needed, and use configured-state metadata
for status checks that do not require secret material.

## Package For Maintenance

Keep the manifest, Vite inputs, source files, and generated `dist/` entries in
sync. Add new contributions to `contributes`, export runtime services from the
extension host, and rebuild before validation.

Declare `bakingrlApi` explicitly and rebuild packages when the contract changes.
Do not rely on SDK version alone for compatibility.

Use `npm run validate` before packing or installing locally. Use
`bakingrl-plugin inspect` when you want to review the manifest surface the
host will enforce.

Document package-owned events, registry keys, cross-package usage choices, and
settings in the package README. That keeps package review and host debugging
tractable.
