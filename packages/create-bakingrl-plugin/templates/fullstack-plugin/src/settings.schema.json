{
  "type": "object",
  "title": "__PLUGIN_NAME__ Settings",
  "properties": {
    "apiBaseUrl": {
      "type": "string",
      "title": "API Base URL",
      "description": "Base URL used by backend connectors.",
      "default": "https://api.example.com"
    },
    "apiKey": {
      "type": "string",
      "title": "API Key",
      "description": "Stored by BakingRL in the OS keychain and only readable by connectors.",
      "format": "password",
      "x-bakingrl-secret": true
    }
  }
}
